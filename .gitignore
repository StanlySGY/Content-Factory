# deps / build
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# env / secrets (real values never committed; use .env.example)
.env
.env.*
!.env.example
!.env.docker.example

# tooling
.ace-tool/
.playwright-mcp/
.DS_Store
*.log

# 大体积非运行时资源（字体包等，不入仓）
Meslo.zip
